<!-- codex-review -->
## Review: SEO measurement, indexing, and column conversion foundations

- Date: 2026-07-17 12:51:21 +09:00
- Mode: Diff / Runtime / Release
- Verdict: PASS

### Critical
- None.

### Major
- None.

### Minor
- FIND-001: The repository now emits the documented custom events to the existing GTM `dataLayer`, but the GTM container is owned by another Google account. Custom-event triggers and GA4 event tags must be configured and published from that account before those events can be confirmed in GA4.
- FIND-002: `column-child-theme/` is not part of the Vercel build. The WordPress child-theme package therefore requires a separate WordPress deployment and live verification; this is an operational release boundary, not a code defect.

### Required Fixes
1. None for commit and Vercel release.

### Evidence
- Scope review: home and diagnosis intent pages, ranking static rendering, shared canonical/navigation paths, analytics bootstrap, WordPress child-theme SEO and contextual CTA changes.
- Independent review: Vercel scope Critical 0 / Major 0 after fixing the root-prefix active-navigation bug and trailing-slash canonical mismatches for `/ranking`, `/kanji`, and `/guide`.
- Command: `npm test`.
- Result: PASS. Analytics data-layer contract and boot initialization tests passed.
- Command: `npm run build`.
- Result: PASS. Generated 17 content pages and 10 kanji pages.
- Command: `npm run build:verify`.
- Result: PASS. 17 source/generated pairs, kanji loop, static assets, SEO content markers, single current-page marker, and three hub canonicals passed.
- Command: `node --check` for the modified build, analytics, boot, ranking, and test scripts.
- Result: PASS.
- Command: PHP 8.3 `-l` for the modified WordPress PHP files.
- Result: PASS for `functions.php`, `header.php`, `footer.php`, `sidebar.php`, and `single.php`.
- Command: `git diff --check`.
- Result: PASS; only the repository's existing LF-to-CRLF notices were emitted.
- Browser QA: home, diagnosis, ranking hub, and both 30-entry ranking pages were checked at desktop and mobile widths with no horizontal overflow or console errors. Ranking pages contained 30 visible static entries and 30-item `ItemList` JSON-LD.
- GTM verification: the existing container receives `dataLayer` custom-event objects without loading a second direct `gtag.js`; container-side trigger/tag publication remains in the owner account.
